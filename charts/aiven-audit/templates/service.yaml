apiVersion: v1
kind: Service
metadata:
  labels:
      {{- include "aiven-audit.selectorLabels" . | nindent 6 }}
  name: {{ .Release.Name }}
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app: {{ .Release.Name }}
  type: ClusterIP
